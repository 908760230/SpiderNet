#include "SpiderNetContext.h"

namespace SpiderNet
{
    Context::Context(const char *name, const char *parm)
    {
    }

    void Context::solveEndless(uint32 handle)
    {
    }

    int Context::push(uint32 handle, Message *message)
    {
    }

} // namespace SpiderNet
